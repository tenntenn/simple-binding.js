define(
    '',
    [
        "sb/binding",
    ],
    function(binding) {

        /**
         * @namespace
         */
        var sb = {
            binding: binding
        };

        return sb;
    }
);
