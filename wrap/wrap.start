(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(['ko'], factory);
    } else {
        // Browser globals
        root.sb = factory(root.ko);
    }
}(this, function (ko) {


